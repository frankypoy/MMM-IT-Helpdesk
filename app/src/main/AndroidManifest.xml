<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="egat.mmm.ithelpdesk">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/apkpic"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NewServiceActivity" />
        <activity android:name=".NewDetailActivity" />
        <activity android:name=".TabMActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".DoingServiceActivity" />
        <activity android:name=".DoingDetailActivity"></activity>
    </application>

</manifest>